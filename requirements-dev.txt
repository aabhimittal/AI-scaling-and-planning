# Development / CI-only dependencies (installed on top of requirements.txt).
-r requirements.txt
pytest>=8.0,<9.0
pytest-cov>=5.0,<7.0
httpx>=0.27,<1.0        # FastAPI TestClient transport
ruff>=0.5,<1.0          # linting + formatting
